import React from 'react';
import ReactDOM from 'react-dom/client'; // or from 'react-dom' if older version
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
